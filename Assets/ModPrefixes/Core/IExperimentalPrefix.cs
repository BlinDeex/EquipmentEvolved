namespace EquipmentEvolved.Assets.ModPrefixes.Core;

/// <summary>
/// Used to tag reforges which are less stable than desired (hard to balance, many incompatible situations etc.)
/// </summary>
public interface IExperimentalPrefix;